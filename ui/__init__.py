"""
UI PACKAGE - Presentation helpers
==================================

Keeps all the "how it looks" code out of app.py so that app.py stays readable
as the wiring layer.

    theme.py   -> CSS and colour palette
    charts.py  -> dependency-free SVG chart builders
    cards.py   -> KPI cards, badges, alert panels
"""
